# ============================================================================
# Python
# ============================================================================
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Virtual environments
venv/
env/
.venv/
env.bak/
venv.bak/
backend/venv/
backend/.venv/

# Packaging / build artifacts
dist/
build/
*.egg-info/
.eggs/

# ============================================================================
# Secrets & environment
# ============================================================================
# Never commit real secrets. `.env.example` is the committed template.
.env
.env.*
!.env.example

# ============================================================================
# Tests, linters, type & coverage caches
# ============================================================================
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
coverage.xml
htmlcov/

# ============================================================================
# Machine learning: local model weights, caches, tracking
# ============================================================================
# Large artifacts are distributed via the HF Hub / a model registry, not Git.
final-pii-model/
models/
*.safetensors
*.pt
*.bin
*.onnx

# Hugging Face / Torch download caches
hf_cache/
model_cache/
.cache/
transformers_cache/

# MLflow local tracking store / artifacts
mlflow.db
mlruns/
mlartifacts/

# ============================================================================
# Frontend (React / Vite / Node)
# ============================================================================


# ============================================================================
# IDE / editor
# ============================================================================
.vscode/
.idea/
*.swp
*.swo
*~

# ============================================================================
# Logs & temp
# ============================================================================
*.log
tmp/
temp/

# ============================================================================
# Personal exploratory fine-tuning track. Code, small OOD test batteries, and
# small report JSONs ARE tracked (see personal_finetune/README.md) -- only the
# large regenerable artifacts are excluded here: full training/eval data
# (already hosted on the farahelmashad/personal-pii-tamerbert-generalization-data
# Kaggle dataset and regenerable via personal_finetune/augmentation/) and the
# large cached raw-prediction dumps from each trial run.
# ============================================================================
personal_finetune/**/__pycache__/
personal_finetune/tamerbert_data/*.jsonl
personal_finetune/*/outputs/**/raw_predictions.json
new_data/
TamerBERT.zip
TamerBERT/

# ============================================================================
# OS generated files
# ============================================================================
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini
